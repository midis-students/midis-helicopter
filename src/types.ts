export type DBLiver = {
    name: string;
    entrance: number;
    floor: number;
    apartment: number;
}

export type DBWorker = {
    name: string;
    job: number;
}

export type DBJob = {
    name: string;
    payment: number;
}